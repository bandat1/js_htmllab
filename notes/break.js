
for(let i = 0; i < 10; i++){
  //if( i == 4 ) break;
  if( i == 4 ) continue;
  console.log(i);
}

/*
Есть такая ф-я Math.random(), которая генерирует случайное число от 0 (включая) до 1 (не включая). 

Задача 1. Написать код, генерирующий случайное целое от n до m

Задача 2. Сгенерировать и вывести карту высот в виде HTML-таблицы

Задача 3. Сформировать ul с набором li, в каждом из которых есть свои ul с li







*/